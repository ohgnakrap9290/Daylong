name: DayLong Push Cron

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger DayLong push cron
        run: curl --fail --silent --show-error https://daylong77.vercel.app/api/cron
